import { Container } from "native-base";
import { Profile } from "../layout/Profile";

export const ProfileContainer = () => {

    return (
        <Container>
            <Profile />
        </Container>
    )
}